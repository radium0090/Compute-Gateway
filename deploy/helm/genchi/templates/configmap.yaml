apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "genchi.fullname" . }}
  labels:
    {{- include "genchi.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.policy | nindent 4 }}
