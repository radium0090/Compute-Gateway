RAX Compute Gateway has been rendered as {{ include "rax-compute-gateway.fullname" . }}.

The chart references the existing Secret {{ .Values.existingSecret | quote }};
it never renders secret values. Wait for the migration hook and all replicas to
be ready, then access the private Service on port {{ .Values.service.port }}.
