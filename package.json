{
  "name": "genchi",
  "version": "0.0.0",
  "private": true,
  "description": "Open-source AI compute gateway",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=9 <10"
  },
  "scripts": {
    "build": "tsc -b --pretty false",
    "clean": "tsc -b --clean",
    "dev": "pnpm --filter @genchi/gateway dev",
    "docs:check": "tsx scripts/validate-docs.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "migrate": "pnpm --filter @genchi/gateway migrate",
    "openapi:breaking": "tsx scripts/check-openapi-breaking.ts",
    "openapi:check": "tsx scripts/generate-openapi.ts --check",
    "openapi:generate": "tsx scripts/generate-openapi.ts",
    "openapi:lint": "tsx scripts/validate-openapi.ts",
    "operations:check": "tsx scripts/validate-operations.ts",
    "performance:check": "tsx benchmarks/gateway-overhead.ts",
    "release:check": "corepack pnpm format:check && corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test:coverage && corepack pnpm test:integration && corepack pnpm test:rc && corepack pnpm build && corepack pnpm openapi:check && corepack pnpm openapi:lint && corepack pnpm openapi:breaking --base origin/main && corepack pnpm sdk:check && corepack pnpm sdk:test && corepack pnpm sdk:test:openai-python && corepack pnpm performance:check && corepack pnpm docs:check && corepack pnpm operations:check",
    "sdk:check": "corepack pnpm sdk:generate && git diff --exit-code -- sdk/typescript/src/generated/schema.ts sdk/python/src/genchi/_generated/models.py",
    "sdk:generate": "openapi-typescript openapi/genchi.openapi.yaml --output sdk/typescript/src/generated/schema.ts --immutable --default-non-nullable false && tsx scripts/generate-python-sdk.ts && prettier --write sdk/typescript/src/generated/schema.ts",
    "sdk:test": "corepack pnpm --filter @genchi-ai/sdk test && PYTHONPATH=sdk/python/src python3 -m unittest discover -s sdk/python/tests -v",
    "sdk:test:openai-python": "python3 sdk/python/compatibility/openai_smoke.py",
    "start": "pnpm --filter @genchi/gateway start",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:live": "vitest run --config vitest.live.config.ts",
    "test:rc": "vitest run --config vitest.rc.config.ts",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": {
    "@genchi/api-contract": "workspace:*",
    "@types/node": "24.13.3",
    "@types/pg": "8.20.3",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "9.39.5",
    "openai": "7.3.0",
    "openapi-typescript": "7.13.0",
    "prettier": "3.9.6",
    "tsx": "4.23.1",
    "typescript": "5.9.3",
    "typescript-eslint": "8.65.0",
    "vitest": "4.1.10",
    "yaml": "2.9.0"
  }
}
